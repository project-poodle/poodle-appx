const express = require('express');
const router = express.Router();
const db = require('../db/db')

const deployment_router = function (req, res, next) {

}

module.exports = {
    deployment_router: deployment_router
}
