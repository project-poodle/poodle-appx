{
    "host": "{{mysql.host}}"
    "port": {{mysql.port}}
    "username": "{{mysql.username}}"
    "password": "{{mysql.password}}"
    "prefix": "{{appx.init.prefix}}"
    "separator: "{{appx.init.separator}}"
}
